{% extends 'index.html.twig' %}

{% block form %}
	{{
    dump(form) 
}}

{% endblock %}
{% block title  %}
	Ajouter Category
{% endblock %}

{% block body %}

	{{form_start(form)}}

	{{form_widget(form)}}

	<button type="submit" class="btn btn-success">Ajouter</button>
	{{form_end(form)}}

{% endblock %}
